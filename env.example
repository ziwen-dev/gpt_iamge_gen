# 复制为 .env 后填写（勿提交真实密钥）
# 勿在 .env 里写 HOST=127.0.0.1，否则 Docker Nginx 无法访问 API
# 监听地址用 GPT_IMAGE_LISTEN（默认 0.0.0.0）

GPT_IMAGE_API_KEY=
GPT_IMAGE_BASE_URL=https://apexapi.roixw.com
GPT_IMAGE_MODEL=gpt-image-2

# 静态页与 API 不同域时启用 CORS：填 * 或逗号分隔的 Origin，例如 https://你的域名
# GPT_IMAGE_CORS_ORIGINS=*
